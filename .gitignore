*.tfvars
*.sops.yaml
secrets/
.idea/
.claude/settings.local.json
.openrouter.env
.devbox/
crash.log

# chart packaging output (package-chart.sh runs from the repo root)
circles-*.tgz

# specs-site build outputs (scripts/specs-build.sh)
.site-src/
specs-site/

# Python virtual env and build artifacts
.venv/
.pytest_cache/
__pycache__/
*.egg-info/

# Bake output (the artifact is built, never committed — CIR-BAKE-ATOMIC-WRITE)
dist/

# Allure raw test results (generated by ci.sh, consumed by evidence generation)
/tmp/allure-raw/

# Browser evidence runs (scripts/test-browser.sh: allure raw + bake per run, never committed)
browser-evidence/
