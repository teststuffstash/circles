[project]
name = "circles"
version = "0.1.0"
description = "circles — the bake + the page (P0 MVP)"
requires-python = ">=3.11"
dependencies = [
    "PyYAML>=6.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]