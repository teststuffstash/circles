image:
  repository: ghcr.io/teststuffstash/circles
  pullPolicy: IfNotPresent
  tag: "" # chart appVersion by default
port: 8080
resources:
  requests: { cpu: 25m, memory: 32Mi }
  limits: { memory: 64Mi }
